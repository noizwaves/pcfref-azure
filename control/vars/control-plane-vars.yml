external_url: https://plane.control.azure.pcfref.com
persistent_disk_type: 10240 # 10gb
network_name: control-plane
azs: ["null"]
vm_type: Standard_F2s
wildcard_domain: plane.control.azure.pcfref.com

cert_pem: ((lets_encrypt_cert.certificate))  # Let's Encrypt cert.pem
private_key_pem: ((lets_encrypt_cert.private_key))  # Let's Encrypt privkey.pem
ca_cert_pem: ((lets_encrypt_cert.ca))  # Let's Encrypt chain.pem